@page
@model HCAMiniEHR.Pages.Patients.CreateModel
@{
    ViewData["Title"] = "Add New Patient";
}

<div class="container mt-4">
    <h1>Add New Patient</h1>

    <div class="card mt-4">
        <div class="card-body">
            <form method="post">
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="Patient.FirstName" class="form-label">First Name</label>
                        <input asp-for="Patient.FirstName" class="form-control" />
                        <span asp-validation-for="Patient.FirstName" class="text-danger"></span>
                    </div>

                    <div class="col-md-6 mb-3">
                        <label asp-for="Patient.LastName" class="form-label">Last Name</label>
                        <input asp-for="Patient.LastName" class="form-control" />
                        <span asp-validation-for="Patient.LastName" class="text-danger"></span>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="Patient.DateOfBirth" class="form-label">Date of Birth</label>
                        <input asp-for="Patient.DateOfBirth" type="date" class="form-control" />
                        <span asp-validation-for="Patient.DateOfBirth" class="text-danger"></span>
                    </div>

                    <div class="col-md-6 mb-3">
                        <label asp-for="Patient.Gender" class="form-label">Gender</label>
                        <select asp-for="Patient.Gender" class="form-control">
                            <option value="">Select...</option>
                            <option value="Male">Male</option>
                            <option value="Female">Female</option>
                            <option value="Other">Other</option>
                        </select>
                        <span asp-validation-for="Patient.Gender" class="text-danger"></span>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="Patient.PhoneNumber" class="form-label">Phone Number</label>
                        <input asp-for="Patient.PhoneNumber" class="form-control" />
                        <span asp-validation-for="Patient.PhoneNumber" class="text-danger"></span>
                    </div>

                    <div class="col-md-6 mb-3">
                        <label asp-for="Patient.Email" class="form-label">Email</label>
                        <input asp-for="Patient.Email" type="email" class="form-control" />
                        <span asp-validation-for="Patient.Email" class="text-danger"></span>
                    </div>
                </div>

                <div class="mb-3">
                    <label asp-for="Patient.Address" class="form-label">Address</label>
                    <textarea asp-for="Patient.Address" class="form-control" rows="2"></textarea>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">Save Patient</button>
                    <a href="/Patients" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
